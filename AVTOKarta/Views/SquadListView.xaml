<UserControl x:Class="AVTOKarta.Views.SquadListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xml:lang="ru-RU">
    <UserControl.Resources>
        <Style x:Key="FieldCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.06" Color="#000000" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <DockPanel>
        <!-- ============ HEADER ============ -->
        <Border DockPanel.Dock="Top" Style="{StaticResource CardBorderShadow}" Margin="16,12,16,10" Padding="20,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Width="42" Height="42" CornerRadius="12"
                        Background="{StaticResource PrimaryLightBrush}" Margin="0,0,14,0"
                        VerticalAlignment="Center">
                    <TextBlock Text="&#x263A;" FontSize="20" Foreground="{StaticResource PrimaryBrush}"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Управление частями" FontSize="18" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Text="Справочник пожарных частей, начальников и старших водителей"
                               FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,3,0,0"/>
                </StackPanel>
                <Button Grid.Column="2" Content="+ Добавить часть"
                        Command="{Binding AddSquadCommand}"
                        Style="{StaticResource SuccessButton}" Padding="14,8" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- ============ DATA TABLE ============ -->
        <Border Style="{StaticResource FieldCard}" Margin="16,0,16,12" Padding="0" ClipToBounds="True">
            <DataGrid ItemsSource="{Binding Squads}"
                      SelectedItem="{Binding SelectedSquad}"
                      CanUserAddRows="False"
                      FontSize="12" RowHeaderWidth="0"
                      AutoGenerateColumns="False"
                      EnableRowVirtualization="True"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      VirtualizingPanel.ScrollUnit="Pixel"
                      ScrollViewer.CanContentScroll="True"
                      CanUserResizeRows="True"
                      BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="200" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Номер" Binding="{Binding Number}" Width="80" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Отряд" Binding="{Binding CrewNumber}" Width="80" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Начальник" Binding="{Binding ChiefName}" Width="180" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Ст. водитель" Binding="{Binding SeniorDriverName}" Width="180" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="120" IsReadOnly="True"/>
                    <DataGridTemplateColumn Header="Действия" Width="80" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="&#x270E;" Style="{StaticResource IconButton}"
                                            Command="{Binding DataContext.EditSquadCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" FontSize="14" Padding="6"
                                            Foreground="{StaticResource PrimaryBrush}" ToolTip="Редактировать"/>
                                    <Button Content="&#x2716;" Style="{StaticResource IconButton}"
                                            Command="{Binding DataContext.DeleteSquadCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" FontSize="12" Padding="6"
                                            Foreground="{StaticResource DangerBrush}" ToolTip="Удалить"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </DockPanel>
</UserControl>
