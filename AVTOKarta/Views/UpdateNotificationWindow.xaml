<Window x:Class="AVTOKarta.Views.UpdateNotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Обновление AVTOKarta"
        Width="420" Height="Auto" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource BackgroundBrush}">
    <DockPanel>
        <!-- Buttons -->
        <Border DockPanel.Dock="Bottom" BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,1,0,0" Padding="16,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Позже" Click="Skip_Click" MinWidth="80" Padding="12,6" FontSize="12"
                        Margin="0,0,8,0"
                        Style="{StaticResource GhostButton}" Foreground="{StaticResource TextSecondaryBrush}"/>
                <Button Content="Обновить" Click="Update_Click" MinWidth="80" Padding="12,6" FontSize="12"
                        Style="{StaticResource PrimaryButton}"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <StackPanel Margin="24,20">
            <TextBlock x:Name="VersionBlock" FontSize="13" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap" Margin="0,0,0,12"/>

            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="14,10"
                    MaxHeight="120" BorderBrush="{StaticResource DividerBrush}" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <TextBlock x:Name="NotesBlock" FontSize="11.5"
                               Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                </ScrollViewer>
            </Border>
        </StackPanel>
    </DockPanel>
</Window>
